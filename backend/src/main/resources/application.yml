server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/beman?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 你的数据库密码
  redis:
    host: localhost
    port: 6379
    password:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.beman.model