<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beman.mapper.PostMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.beman.model.Post">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="anonymous_id" property="anonymousId" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="content_type" property="contentType" />
        <result column="voice_url" property="voiceUrl" />
        <result column="tags" property="tags" />
        <result column="view_count" property="viewCount" />
        <result column="like_count" property="likeCount" />
        <result column="comment_count" property="commentCount" />
        <result column="status" property="status" />
        <result column="is_top" property="isTop" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 分页查询帖子列表 -->
    <select id="selectPostPage" resultMap="BaseResultMap">
        SELECT 
            p.*,
            u.nickname as author_name,
            u.avatar as author_avatar
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        WHERE p.deleted = 0 AND p.status = 1
        <if test="tags != null and tags.size() > 0">
            AND (
            <foreach collection="tags" item="tag" separator=" OR ">
                p.tags LIKE CONCAT('%', #{tag}, '%')
            </foreach>
            )
        </if>
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%') 
                 OR p.content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="contentType != null">
            AND p.content_type = #{contentType}
        </if>
        ORDER BY p.is_top DESC, p.create_time DESC
    </select>

    <!-- 根据ID查询帖子详情 -->
    <select id="selectPostDetail" resultMap="BaseResultMap">
        SELECT 
            p.*,
            u.nickname as author_name,
            u.avatar as author_avatar
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        WHERE p.id = #{id} AND p.deleted = 0 AND p.status = 1
    </select>

    <!-- 更新帖子浏览次数 -->
    <update id="updateViewCount">
        UPDATE post 
        SET view_count = view_count + 1 
        WHERE id = #{id}
    </update>

    <!-- 更新帖子点赞次数 -->
    <update id="updateLikeCount">
        UPDATE post 
        SET like_count = like_count + #{increment} 
        WHERE id = #{id} AND like_count + #{increment} >= 0
    </update>

    <!-- 更新帖子评论次数 -->
    <update id="updateCommentCount">
        UPDATE post 
        SET comment_count = comment_count + #{increment} 
        WHERE id = #{id} AND comment_count + #{increment} >= 0
    </update>

</mapper>
